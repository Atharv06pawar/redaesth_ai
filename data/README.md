# Data

This directory holds the managed dataset lifecycle for the repository: raw downloads, cleaned conversations, scoring outputs, synthetic data, retrieval documents, and final train/validation/test splits. Large generated artifacts are ignored by git.
