# Retrieval Corpus

This directory stores the scientific knowledge base used at inference time. It should contain curated documents, chunked corpora, vector indexes, and related manifests rather than raw conversational training data.
