# Source Package

Shared Python runtime code lives under `src/`. This package should contain reusable utilities, configuration loading, logging, CLI commands, and common abstractions used by the spec-mandated scripts.
