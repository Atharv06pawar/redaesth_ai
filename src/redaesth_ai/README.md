# `redaesth_ai` Package

This package provides the shared application runtime for the repository. Keep reusable logic here and let top-level phase scripts act as thin, specification-aligned entrypoints.
