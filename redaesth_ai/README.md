# Root Package Shim

This small package exists only to make `python -m redaesth_ai.cli` work from a fresh checkout without requiring an editable install first. The real implementation remains under `src/redaesth_ai/`.
